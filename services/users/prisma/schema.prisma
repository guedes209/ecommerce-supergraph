datasource db {
  provider = "sqlite"
  url      = "file:./users.db"
}

generator client {
  provider = "prisma-client-js"
  output   = "./generated/client"
}

model User {
  id       String @id @default(uuid())
  username String @unique
}

