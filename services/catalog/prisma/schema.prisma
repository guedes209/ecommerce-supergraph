datasource db {
  provider = "sqlite"
  url      = "file:./catalog.db"
}

generator client {
  provider = "prisma-client-js"
  output   = "./generated/client"
}

model Product {
  id    String @id @default(uuid())
  name  String
  price Float
}

